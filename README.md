# Ikasan Integration Module Sample with Spring Boot

* How to run:

```bash
mvn clean install
cd target
java -jar springbootsample-1.0-SNAPSHOT.jar
```

* Login to blue console (admin/admin):
http://localhost:18080/
